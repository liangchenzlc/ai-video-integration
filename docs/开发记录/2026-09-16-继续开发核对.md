# 剩余开发核对

本轮授权：依据现有产品规格、模块设计和交接继续完成剩余工作；遇到影响产品或验收判断的不确定项向用户确认。旧交接中“本轮只做 T01”的限制属于上一轮，不限制本轮。

## 接手时核对

- 接手提交 `2f36f9d`，工作区原本干净；开发分支 `feat/remaining-work`。
- 已阅读 README、T01 最终验证和详细交接、产品规格、制作交互、开发验收、跨模块约定、T02 设计和设计 SQL。
- 源码只有 runtime health/capabilities、桌面监督进程、有限桥和未开放业务页面。T02–T14 均不能记为完成。
- 原 T01 测试记录是前一机器/会话的证据，本机仍需重现。本机原有 Node 20.19.5、Python 3.13.15，缺项目 `.tools`、虚拟环境、node_modules 和 release。
- 官方 Node/Electron/FastAPI 固定版本元数据查询成功。正按固定版本恢复工具链和 frozen 依赖，不改锁文件。

## 依赖顺序与完成标准

| 批次 | 工作 | 通过依据 |
| --- | --- | --- |
| T02-A | 创建、打开、最近项目、会话和跨库恢复 | 真目录/SQLite，重复提交、非空目录保护、中断恢复、移动、损坏库、第二写者只读 |
| T02-B | 草稿与保存反馈 | 800ms 防抖、flush、正文 hash、CAS、回执、防响应丢失重写，失败保留输入 |
| T02-C | 导入、媒体访问、重新定位、本地作业 | FFmpeg 探测、流式复制、Range、路径边界、取消、崩溃恢复、backup 一致性 |
| T03 | 加密凭据与阶段配置 | DPAPI、秘密不泄漏、能力/连接/效果区分、离线准备 |
| T04–T06 | 任务费用、版本依赖、文字图像适配 | 三重预算、unknown 不重发、单执行槽、采用撤销、全部候选/下载恢复 |
| T07–T09 | 故事资产分镜、配音字幕、静态时间线 | 原文与必留要求、稳定编号、独立字幕、实测容量、统一 RenderPlan、七镜预演 |
| T10–T11 | 动态与声音 | 确切能力适配、正式音频传输、对白与动作、音乐音效及混音 |
| T12–T13 | 检查导出、安装升级 | 当前采用链、检查证据、MP4 完整解码、故障保留数据、干净系统验证 |
| T14 | 真实发布验收 | 六条 28–32 秒样片、两名独立评审、五名新手至少四名独立完成、同质量费用/时间配对比较 |

逐批执行实现、验证、Review、记录，再继续。现有设计是实现依据，不另改首版范围。工程通过不等于模型效果或发布通过。

## 需在相应阶段确认的外部条件

真实模型账户/地域/型号和明确收费预算；正式声音传输账户；写实动态效果反馈；真人评审与新手参与者；分发主体/证书与发布授权。基础本地开发不依赖这些输入。不得把假服务、研究素材或合成测试当成上述验收。

## 进度

- [x] 阅读与源码核对
- [x] 本机重现 T01 基线
- [x] T02-A
- [x] T02-B
- [x] T02-C
- [x] T03 本地配置与凭据安全工程（真实模型/账户准入单列）
- [x] T04 本地任务、预算、费用与恢复工程（后续采用/剪辑费用联动仍须复验）
- [x] T05 本地版本采用、依赖、结构检查、确认与撤销工程
- [ ] T06–T14（按上表拆后续实施计划）

T02-A 已完成真实目录/SQLite、API/有限桥/桌面集成；六项桌面测试通过，后续复查发现的七项会话/恢复问题均已修复并通过定向测试与复查。T02-B 的草稿服务通过 30 项新增存储测试，全部存储回归 78 项通过；集成后的后端全套 237 项通过，前端单元测试 98 项通过。后续退出间隙修复由 12 项定向测试和独立 Review 通过。真实桌面已验证连续编辑、立即关闭项目、移动后重开、清空原文后立即退出、重开空稿；保存准确递增三次。另一个桌面测试通过项目切换等待锁定、原生退出保存后仍锁定、取消退出恢复编辑。截图在 `.cache/t02-desktop/0a7c3039-e18a-4954-b0c5-fd48001cbe86/project-home.png`。测试以 `--user-data-dir` 隔离，不依赖 Windows APPDATA 环境变量覆盖。

T02-C 已实现本机工具选择/校验、六种格式的完整解码导入、作业恢复/取消、稳定媒体索引、私有流式预览、同哈希重定位和 WAL 备份。全量脚本后端 290 项、前端单元 150 项通过，API 61 项通过，类型/静态/契约与生产构建通过。桌面初次 6 项通过、2 项发现 React 相邻组件键重复；两行根因修复后，两个原失败用例原样通过（17.4 秒），未降低断言。真实媒体场景验证工具配置、导入、丢响应查询/原编号重试、无重复作业、预览解码、目录移动后重开。截图已检查：`.cache/t02-media-desktop/7d085a47-b9ab-4ee8-8524-c15d66ac3e18/media-desktop.png`。三项存储和两项集成审查问题均已修复并通过复审。

T03 已实现用户范围 DPAPI、临时凭据、秘密输入即时清空、独立设置 CAS、安全只读详情、OSS 配置、能力三状态与免费检测边界、阶段配置和 003 项目迁移。既有v2项目写前备份、只读不升级，新建直接003；迁移失败回滚且保留备份，并验证备份后项目身份被替换时拒绝迁移。真实 DPAPI/settings 9 项、其他存储 126 项通过；集成后的后端全套 314 项、前端单元 155 项通过，类型/静态/生产构建通过。真实桌面初跑8项通过、2项暴露桥白名单与下拉标签问题；更新精确九方法白名单、修复五个select的label关联后，三项定向场景均通过。完整设置场景实测持久重开、OSS配置重开修改、确认删除、临时凭据重启消失、未知结果只查询原编号。截图已查看：`.cache/t03-desktop/e5011a59-c541-429c-8a99-a74f4b3137a2/` 的三张视口图。

T03 的生产能力目录与免费检测适配器默认空，只有测试注入固定档案与免费检查器。未验证的型号/账户/音色/价格不伪填为可用；真实官方适配、账户与效果证据继续由 T06/T08/T10/T11/T14 准入。离线故事/媒体准备可用，完整示例制作在后续内容模块落实。当前不发起收费调用。

T04 已实现 004 迁移、三重预算、冻结计划和价格、单任务执行槽、prepared/submitting 持久边界、原操作查询、未知/下载恢复、结算更正与稳定分页。真实子进程中断验证四个窗口：prepared 不自动发送、受理后丢响应不重发、持久远端编号只查询、持久结果只恢复下载。独立审查发现的顶层媒体快照遗漏、失效项目目录阻断全局活动，以及联调发现的游标不一致均已修复并复审通过。

修复版完整 `scripts/check.ps1` 退出 0：后端 367 项（含 API 74 项）、前端 163 项、真实 Electron 11 项通过，类型/静态/格式/契约与生产构建通过。随后将原始 JSON 预览改为中文内容，并补充原文哈希/Unicode 范围校验，防止旧引用产生伪摘录；最新前端全套 170 项、构建和任务 Electron 定向场景通过，独立复审通过。最新预览截图已检查：`.cache/t04-desktop/a1b0fb59-d43b-4384-8b25-622817ae7ab7/tasks-plan.png`。日志：`.cache/t04-final-check.log`、`.cache/t04-preview-check.log`。

T04 当前只有明确标识的本地 synthetic 适配器，没有真实收费请求；真实供应商准入属于后续模块。结构化结果保存在调用记录中，正式候选版本接入 T05/T06。未配置明确返工情景时该估算为 0，未提供情景编辑器；裁剪/采用/撤销接入后仍须复验费用不回退与补拍独立计费。测试通过不替代这类后续集成或真实效果验收。

当前按 `docs/superpowers/plans/2026-09-16-t05-versions.md` 集成不可变版本、采用影响、必要结构检查、确认和撤销。API 与后端独立审查已通过，语义无关上游变更误判 stale 已修复。最新后端完整检查 401 项通过（API 81 项），类型/静态/格式和两个Python打包通过，日志 `.cache/t05-backend-final-check.log`。同窗口回执精确推进干净草稿基线已通过正常桌面场景，且采用/撤销保留实际费用已有回归。

T05 前端初次冻结版 184 项单元测试通过；全桌面 12 个场景中，11 项初跑通过，1 项仅因桥方法白名单断言未更新而失败，补入 versions 九方法后原用例通过。独立前端复核发现的四项边界问题现已全部修复并复审通过：桥层复用完整正式候选Schema、检查作业查询失败后可读取原作业、草稿引擎加载期间拦截命令、回执绑定原操作编号。修复后前端全套 191 项通过，类型/格式/构建通过；更新后的版本桌面场景 1 项通过（14秒），包含查询失败后只取原作业、检查提交仍只有一次、重开报告恢复和加载期间禁写。最新截图 `.cache/t05-desktop/e6041297-2bb4-45c7-a88a-0f06e3a1d352/versions-confirmed.png` 已检查。后端、API、前端独立审查均通过，T05 本地工程完成；T07/T09 新增承载/时间线/裁剪/补拍关系时仍须复验相应联动。

用户于本轮要求暂停开发并提交推送当前成果。T06 目前只有 `docs/superpowers/plans/2026-09-16-t06-candidates.md` 实施计划和[官方资料复核](./2026-09-16-T06官方资料复核.md)，候选持久登记与安全下载尚未落地。恢复开发时从 T06 开始，不重复实现 T02–T05。尚未发起真实推理、启用未验证能力或产生模型费用。
