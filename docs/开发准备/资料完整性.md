# 资料完整性与工程替代输入

核对日期：2026-09-15。当前工作区有 `.git`，无 `.local`。本记录限定于本次可访问目录，不推断历史文件已被删除。

## 1. 当前可复核范围

已有 15 份 Markdown、8 份 JSON、2 份调用/进度 CSV、参考制作 TXT、研究图片与四段 MP4。JSON 可解析不表示满足接口语义。调用表 25 行为 24 次云端推理与 1 次本地临时配音；无赠额估价合计 6.1507179 元，与附录一致，实际云端结算未知。

文档中指向缺失文件的 10 个本地链接均在 06：七张独立关键帧、D01.wav、animatic/manifest.json、参考 inventory.csv。调用表 25 个 output_path 均指向当前缺失的历史目录。部分候选、对照图及视频存在 `docs/资料` 副本，不能据此宣称原始请求和全部候选已经恢复。

## 2. 工程验证素材包

本轮新增的领域样例、接口样例、SQLite 插入数据、预算和状态序列均为人工构造的 synthetic 输入，目的是验证字段、关系、单位和业务预期。媒体可使用仓库既有研究 MP4 做播放测试，但必须明确它尚未通过质量验收。

离线原型不读取 API Key、不保存真实项目、不调用服务商；所有金额只在模拟状态中显示。生成的静音 WAV 或色块素材如用于探测，其来源记录为本地合成，不能替代自然配音和视觉质量证据。

## 3. 历史复现材料的最小补齐包

每次研究按稳定 attempt_id 组织：

1. 脱敏请求及响应，确切 provider/model/region，时间、返回用量、远端任务状态。
2. 输入文本、参考图片/音频及其 SHA-256，模板和适配版本；签名链接换成逻辑输入引用。
3. 全部候选文件、失败记录、实际媒体探测信息和采用决定。
4. 估价与结算依据分别保存；账单截图遮蔽账户及无关消费。
5. 本地构建脚本、依赖版本、清单及命令；缺失项逐项写 unknown。

不收集或提交 Key、DPAPI 密文、仍有效的签名 URL。原件无法找回不伪造补录；新实验使用新 ID，不覆盖旧表。

## 4. 文档链接策略

06 的历史路径保留用于以后恢复，同时通过本记录说明当前缺失。验证脚本将这 10 个已知链接列为预期历史缺口；新增断链仍算失败。源码目录、生产 lockfile 和正式运行报告尚不存在是待开发事实，不是需要编造的资料。
